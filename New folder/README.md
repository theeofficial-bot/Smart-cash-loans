STUDENT NAME: MWANGO MALAMA
STUDENT ID:2410990864
GITHUB REPOSITORY:




          QUESTION 2: HTML Elements

1.
   The five HTML elements I found most challenging were <form>, <table>, <select>, <textarea> and <nav>.
I found the <form> element challenging because I had to understand how different input fields work together. I used different types of inputs for information such as the user's name, phone number, email and loan amount.
The <table> element was challenging because I had to arrange information correctly into rows and columns using <tr>, <th> and <td>.
The <select> element was challenging because I had to create a list of loan types that the user could choose from.
The <textarea> element was used for allowing the user to provide more information about the purpose of the loan.
The <nav> element was challenging at first because I had to connect the navigation links to the different sections of my website.

2.
  I used semantic HTML elements to organize the website into clear sections. I used <header> for the main heading and introduction, <nav> for the navigation links, and <section> for different parts of the website such as the home, about, loan services, application and contact sections.
I used <footer> at the bottom of the website for the company information and copyright information. Using these elements made the structure of my website easier to understand and organize.

3. 
  The <section> element was the most useful for organizing my layout because I used it to separate the different parts of the website. This made my content easier to organize and also made my HTML code easier to read.
Question 3: HTML Attributes

    QUESTION 3: 

  The three attributes I found essential were href, src and id.
The href attribute was important because I used it for navigation links. The src attribute was used to display images on the website. The id attribute was important because I used it to identify different sections of the website and connect them to the navigation links.

2. 
  I used the id attribute to give specific sections of my website unique names, such as home, about, loans, apply and contact.
The class attribute is normally used when the same group or styling needs to be applied to more than one element. Since I was creating the website using HTML only, I mainly used id attributes rather than classes.
3. 
  The href attribute helped improve the user experience the most because it allowed visitors to move between different sections of the website using the navigation links. This means that users do not have to scroll through the whole page to find the information they need.
    QUESTION 4: 
1. 
  Before coding, I first decided what type of website I wanted to create. I chose to create a loan company website because it allowed me to include different types of information and HTML elements.
I planned the main sections to include the home section, information about the company, loan services, loan packages, a loan application form and contact information.
After planning the sections, I started creating the HTML structure and then added headings, paragraphs, images, links, lists, tables and form elements.

2.
    What was your approach to testing and debugging your HTML?
I tested my website by opening the HTML file in a web browser after making changes. I checked whether the headings, paragraphs, images, tables, navigation links and forms were displaying correctly.
When something did not work, I went back to the HTML code and checked for mistakes such as missing closing tags, incorrect attributes or incorrect links. After correcting the problem, I saved the file and refreshed the browser to check it again.

3.
  One challenge I faced was getting some of the images to display correctly. I checked the image source and the HTML code to make sure the image links were correct.
Another challenge was organizing the different sections of the website. I overcame this by using semantic elements such as <header>, <nav>, <section> and <footer>.
I also had to make sure that the navigation links were connected to the correct sections. I checked that the href values matched the correct id values.

    QUESTION 5: 
1. 
   The Git commands I used during development included:
git init — I used this to initialize my project as a Git repository.
git add . — I used this to add my project files to the staging area.
git commit -m "message" — I used this to save changes to my project with a message describing the changes.
git status — I used this to check the current state of my files.
git log --oneline — I used this to view my previous commits.
git branch — I used this to check the branch I was working on.
git remote -v — I used this to check the GitHub repository connected to my project.
git push — I used this to upload my local project changes to GitHub.
2. 
   I made commits as I worked on different parts of my project. I used commit messages that described the changes I had made, such as adding the website or updating the README file.
I tried to make the commit messages simple and related to the changes I made so that I could understand the progress of my project later.
Important: Before you submit, check your actual number of commits with:
git log --oneline
Then replace the first sentence with the exact number shown by Git.

3. 
  Version control is important because it keeps track of changes made to a project. It allows me to save different versions of my work and makes it easier to see what has changed.
It is also useful if something goes wrong because previous versions of the project can be checked. GitHub also makes it possible to store the project online and share the project with other people.

      QUESTION 6: 

1. 
   I checked my HTML carefully to make sure that my opening and closing tags were correct.
I tested the website in a web browser to make sure that the different sections displayed correctly. I also checked my links, images, tables and form fields.
When I found an error, I went back to the code, corrected it and tested the website again.

2.
   I organized my HTML into different sections so that it was easier to understand. I used indentation to make elements inside other elements easier to identify.
I used headings to separate different parts of the website and used semantic elements such as <header>, <nav>, <section> and <footer>.
I also used meaningful names for my id attributes so that I could understand what each section represented.

3.
   If I had more time, I would improve the appearance of the website by adding CSS. I would also make the website more responsive so that it works better on phones, tablets and computers.
I would improve the loan application form and add more useful features. I would also improve the images and make sure they are properly optimized.
Another improvement would be adding more information about the loan services and making the website easier to navigate.
One thing to fix before you paste this into your README: Question 5.2 needs your actual commit number. The assignment specifically asks for how many commits you made, so we shouldn't invent that number. 
